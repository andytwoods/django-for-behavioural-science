<!-- Terms that should be a hyperlink wherever they appear in the tutorial's prose.

     One line each, in Markdown's reference-link syntax. scripts/autolink.py reads this
     file and wraps every bare mention, so a term is linked in one place rather than at
     each of its sightings. Code blocks, inline code, headings and text that is already
     a link are left alone.

     Matching is exact and case-sensitive, and stops at a word boundary: "jsPsych" is
     linked, "jsPsychHtmlKeyboardResponse" is not. -->

[jsPsych]: https://www.jspsych.org/
[Docker]: https://docs.docker.com/get-started/docker-overview/
